# 2026-09-10 语聊视觉校正记录

状态：结构校正已实施，尚未通过一比一视觉验收。

参考：上级目录 01-core-pages.png、02-invites-management.png。

本轮修复：大厅创建按钮恢复标题下方；房间卡片改为 198:260 比例并拆出头像/人数底栏；建房改全屏、字段计数移至输入框下方、游戏选择卡片及开关重新排版；房内状态并排、麦位间距与白底游戏卡片重排；邀请列表和房间管理改为独立全屏，成员与上麦审批改为分组入口。

## 浏览器证据边界

- create-browser.png、lobby-browser.png、room-browser.png、management-browser.png 为真实组件生成的 HTML 在 Mac Chromium 中渲染，内层视口 393×852。外层工具截图为 520×953，右侧蓝色区域不是产品页面。
- 为避免创建业务数据，使用隔离房间/头像测试数据，没有修改账号或调用建房接口。截图不是用户已登录会话，也未验证 RTC。
- 独立浏览器访问现有开发服务被门禁正确跳转至登录页；登录初始化的 /auth/me 返回 401。未启动任何开发服务，未执行 production build，未安装依赖。
- 大厅四张卡片均为 180×236；房内与管理页宽度为 393，无水平溢出；建房按钮底边 836，管理页关闭按钮底边 770，均在视口内。建房游戏选择器边框为 0，已消除重复内框。

## 仍有差异

- 部分图标与按钮仍使用此前通用图集/CSS 材质，未达到批准稿的像素一致。
- 新建房头图为生成式提取，不是原稿无损图层；用途和完整提示词见 header-asset.json。
- 不伪造原稿中的房间码查询入口：当前前端没有已确认的全局房间码查询接口。
- 房间容量依据后端配置，创建前不硬编码为 8；真实房间使用返回的容量。
- RTC 状态没有接入，因此显示“语音尚未连接”，不能把 HTTP 房间数据读取成功显示成“语音已连接”。权限/重连页仍待与负责语音功能的会话衔接。
- 用户实际登录页面与不同机型的完整视觉验收仍待完成，不能据本轮 lint 或隔离截图宣布一比一通过。
